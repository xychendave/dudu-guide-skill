# v0.1 验证记录

日期：2026-09-05。以下记录描述实际验证范围，不代表在所有 Agent、浏览器或真实园区都已验证。

## 自动验证

本地使用 Node.js 26.5.1 运行 9 项测试，全部通过。GitHub Actions 配置在 Node.js 20、22、24 上运行同一套测试与示例地点包校验；远端结果见仓库 Actions。

- 数据引用、日期、危险来源协议与演示/真实模式边界。
- 同点距离、单向路径、断开连接、关闭节点与无障碍筛选。
- 多种时间预算下，路线总时间包含步行、停留、出口和缓冲。
- 中途从当前位置重排，排除已访问和未选择的站点。
- 过期事实不进入讲解与 Agent 上下文。
- Markdown 导出保留未目击状态并转义活动标记。
- 备份合并保留原记录、拒绝不同地点和畸形记录。
- CLI 生成、校验、改名保持演示状态与拒绝覆盖。
- 本机服务拒绝私有文件、站外 Host、写请求与目录外符号链接。

Skill 已通过创建工具的格式校验；文档相对文件链接已检查。

## 浏览器检查

在本机 Chrome 中实际验证：页面启动、生成路线、手动到馆、未遇见替代建议、文字记录保存、刷新后持久保存、中途修改到 45 分钟后的重排，以及 Markdown/JSON 下载。下载文件已读取检查，未目击与笔记内容得到保留。

检查了桌面界面和 390 像素宽度的响应式布局；该宽度下没有页面横向溢出。页面未报告 JavaScript 错误。README 的工作台截图来自本次运行，内容均为虚构测试数据。

浏览器扩展未启用文件 URL 访问，因此文件选择器的备份导入没有完成端到端回测；对应的解析、校验、合并和防覆盖逻辑有自动测试。这里明确保留该验证缺口。

## 尚未验证或未提供

没有对真实园区的路线与知识做实地验证；没有宣称模板的估计时间可直接用于红山。没有测试所有 Agent 的自动发现、Windows 本机环境、Safari、PWA 离线缓存或大规模多人服务。

模型、语音、图像识别、共享观察与 GPS 不在本地模板默认功能中，因此没有把红山线上服务的能力算作本模板已通过的测试。
