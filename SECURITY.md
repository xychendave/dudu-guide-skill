# 安全问题反馈

请不要在公开 Issue 中粘贴密钥、密码、个人记录或可用于攻击真实服务的细节。使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口（维护者启用后可用）。如果入口暂不可用，先提交不含敏感细节的 Issue 请求私密联系渠道。

有用的信息包括：受影响版本、触发条件、影响范围、最小复现及建议修复。复现使用虚构地点和测试数据，不对生产红山服务进行破坏性验证。

v0.1 提供本机静态工作台，不包含用户账号、云端数据库和模型后端。新增远程服务时，应分别评估它的数据与权限边界。
